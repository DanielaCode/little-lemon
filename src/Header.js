import React from 'react';
import Nav from './Nav';

const Header = () => {
    return (
        <header>
            <img/>
            <Nav></Nav>
        </header>
    );
};

export default Header;